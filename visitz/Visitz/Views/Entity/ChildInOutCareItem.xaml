<?xml version="1.0" encoding="utf-8" ?>
<ContentView 
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:models="clr-namespace:Visitz.Models"
    x:Class="Visitz.Views.Entity.ChildInOutCareItem"
    x:DataType="models:FamilyMember">

    <VisualStateManager.VisualStateGroups>
        <VisualStateGroupList>
            <VisualStateGroup x:Name="CommonStates">
                <VisualState Name="Normal">
                    <VisualState.Setters>
                        <Setter TargetName="NameLabel" Property="BackgroundColor" Value="Transparent" />
                        <Setter TargetName="ItemCheckBox" Property="CheckBox.IsChecked" Value="False" />
                        <Setter Property="BackgroundColor" Value="Transparent" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState Name="Selected">
                    <VisualState.Setters>
                        <Setter TargetName="NameLabel" Property="BackgroundColor" Value="{StaticResource ItemSelectedColor}" />
                        <Setter TargetName="ItemCheckBox" Property="CheckBox.IsChecked" Value="True" />
                        <Setter Property="BackgroundColor" Value="{StaticResource ItemSelectedColor}" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateGroupList>
    </VisualStateManager.VisualStateGroups>

    <HorizontalStackLayout>
        <CheckBox x:Name="ItemCheckBox" />

        <Label
            x:Name="NameLabel"
            BackgroundColor="White"
            HorizontalOptions="Start"
            VerticalOptions="Center"
            VerticalTextAlignment="Center"
            Padding="10"
            Text="{Binding FullDisplayName}" />
    </HorizontalStackLayout>
    
</ContentView>
